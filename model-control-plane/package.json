{
  "name": "@hermes/model-control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/main.js",
    "test": "tsx --test test/*.test.mjs test/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/main.ts"
  },
  "dependencies": {
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "26.x",
    "tsx": "^4.21.0",
    "typescript": "^7.0.2"
  }
}
